import React, { useState, useEffet, useContext } from 'react';
import {
  StyleSheet,
  View,
  Text,
  TouchableOpacity,
  ImageBackground,
} from 'react-native';

const styles = StyleSheet.create({
  container: {

  }
});

export const ProfileStoryScreen = () => {
  return (
    <View>
      <Text>Hello World</Text>
    </View>
  );
};